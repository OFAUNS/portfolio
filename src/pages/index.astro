---
import Layout from "@/layouts/Layout.astro";
import { getCollection } from "astro:content";
import ThemeToggle from "@/components/ThemeToggle.astro";
import PortfolioScene from "@/components/PortfolioScene";
import ResponsiveImage from "@/components/ResponsiveImage.astro";
import { getProjectPresentation } from "@/lib/projectPresentation";
import { getLargestVariant } from "@/lib/generated/image-manifest";
import { getBalancedTitleLines } from "@/lib/titleBreaks";

// --- CONFIGURATION ---
// 1 = Center Aligned (Original)
// 2 = Left Aligned (Bax Style)
// 3 = Split View (Modern Sidebar)
// 4 = Bento / Mosaic View
// 5 = Cinematic Scroll (Full Screen Snap)
const CURRENT_LAYOUT: number = 3;

// ---------------------

const PROFILE = {
    name: "OFAUNS",
    role: "Visual Designer / Motion, Web & Interaction",
    email: "cheny4314@gmail.com",
    bio: "Building cinematic visual systems, motion pieces, web interfaces, and interactive design with a sharp eye for atmosphere and detail.",
    avatar: "/header.jpg",
    links: {
        twitter: "https://x.com/baxartworkz",
        email: "mailto:cheny4314@gmail.com",
    },
};

const SCENE_COLORS = ["#5ddcff", "#8fb4ff", "#f2f6ff", "#9ff3d1", "#ffb39a"];

// Fetch all work items (mdx files)
const portfolioData = await getCollection("work");

// Sort by date descending
const sortedPortfolio = portfolioData.sort(
    (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
);

// Group by genre
const groupedPortfolio = sortedPortfolio.reduce(
    (acc, project) => {
        const genre = project.data.genre || "Motion";
        if (!acc[genre]) acc[genre] = [];
        acc[genre].push(project);
        return acc;
    },
    {} as Record<string, typeof sortedPortfolio>,
);
const genreNames = Object.keys(groupedPortfolio).sort((genreA, genreB) => {
    const aIsFilm = genreA.toLowerCase() === "film";
    const bIsFilm = genreB.toLowerCase() === "film";
    if (aIsFilm === bIsFilm) return 0;
    return aIsFilm ? 1 : -1;
});
const orderedGroupedPortfolio = genreNames.map((genre) => [
    genre,
    groupedPortfolio[genre],
] as const);
// Flatten groupedPortfolio to respect genre ordering for Layout 5
const genreSortedPortfolio = orderedGroupedPortfolio.flatMap(([, projects]) => projects);
const genreMeta = genreNames.reduce(
    (acc, genre, index) => {
        acc[genre] = {
            color: SCENE_COLORS[index % SCENE_COLORS.length],
            index: index + 1,
        };
        return acc;
    },
    {} as Record<string, { color: string; index: number }>,
);
const featuredSceneProjects = genreSortedPortfolio.slice(0, 7).map((project) => {
    const genre = project.data.genre || "Motion";

    return {
        title: project.data.title,
        genre,
        thumbnail: getLargestVariant(project.data.thumbnail),
        color: genreMeta[genre]?.color || SCENE_COLORS[0],
    };
});
---

<Layout title="OFAUNS Portfolio" description={PROFILE.role} image="/og-image.png">
    {
        /* =========================================
        LAYOUT 1: Centered / Original
       ========================================= */
    }
    {
        CURRENT_LAYOUT === 1 && (
            <main class="container mx-auto px-4 py-12 max-w-6xl animate-blur-in">
                {/* Profile Section */}
                <header class="flex flex-col items-center justify-center mb-16 space-y-6 relative">
                    <div class="absolute right-0 top-0 md:top-auto">
                        <ThemeToggle />
                    </div>

                    <div class="profile-flip-container w-24 h-24 md:w-32 md:h-32 relative cursor-pointer [perspective:1000px] z-50 group hover:scale-110 transition-transform duration-500">
                        <div class="profile-flip-inner w-full h-full relative transition-[transform] duration-700 ease-in-out [transform-style:preserve-3d]">
                            <ResponsiveImage
                                src={PROFILE.avatar}
                                alt="Profile"
                                class="object-cover w-full h-full absolute inset-0 bg-muted [backface-visibility:hidden]"
                                decoding="async"
                                fetchpriority="high"
                                loading="eager"
                                sizes="8rem"
                            />
                            <ResponsiveImage
                                src="/melogo.png"
                                alt="Logo"
                                class="object-cover w-full h-full absolute inset-0 bg-white [backface-visibility:hidden] flip-180"
                                loading="lazy"
                                decoding="async"
                                sizes="8rem"
                            />
                        </div>
                    </div>

                    <div class="text-center max-w-lg space-y-4">
                        <h1 class="text-2xl font-bold tracking-tight animate-blur-in delay-200">
                            <a href="/about" class="hover:text-primary transition-colors">
                                {PROFILE.name}
                            </a>
                        </h1>
                        <p class="text-muted-foreground leading-relaxed animate-blur-in delay-300">
                            {PROFILE.bio}
                        </p>
                        <div class="flex gap-4 justify-center text-muted-foreground text-sm pt-2 animate-blur-in delay-400">
                            <a
                                href={PROFILE.links.twitter}
                                class="hover:text-foreground transition-colors"
                            >
                                Twitter
                            </a>
                            <a
                                href={PROFILE.links.email}
                                class="hover:text-foreground transition-colors"
                            >
                                Email
                            </a>
                        </div>
                    </div>
                </header>

                {/* Grouped Video Grid */}
                <div class="space-y-16">
                    {orderedGroupedPortfolio.map(
                        ([genre, projects]) => (
                            <section class="space-y-6">
                                <h2 class="text-xl font-semibold tracking-wider uppercase mb-6">
                                    {genre}
                                </h2>

                                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                                    {projects.map((project, index) => (
                                        <a
                                            href={`/work/${project.slug}`}
                                            class={`group block space-y-3 cursor-pointer animate-blur-in`}
                                            style={`animation-delay: ${Math.min((index + 1) * 100, 600)}ms`}
                                        >
                                            {/* Thumbnail Container with 16:9 Aspect Ratio */}
                                            <div class="relative aspect-video overflow-hidden bg-muted group-hover:opacity-90 transition-opacity">
                                                <ResponsiveImage
                                                    src={project.data.thumbnail}
                                                    alt={project.data.title}
                                                    class="object-cover w-full h-full transition-transform duration-500 group-hover:scale-105"
                                                    loading="lazy"
                                                    decoding="async"
                                                    sizes="(max-width: 768px) 92vw, (max-width: 1200px) 44vw, 30vw"
                                                />
                                                <div class="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
                                                    <span class="text-white font-medium tracking-wide">
                                                        VIEW PROJECT
                                                    </span>
                                                </div>
                                            </div>

                                            {/* Info */}
                                            <div class="space-y-1">
                                                <h3 class="font-medium text-lg leading-none group-hover:text-primary transition-colors">
                                                    {project.data.title}
                                                </h3>

                                                <div class="flex items-center justify-between">
                                                    <p class="text-sm text-muted-foreground">
                                                        {project.data.client} •{" "}
                                                        {new Date(
                                                            project.data.date,
                                                        ).getFullYear()}
                                                    </p>

                                                    {/* Roles */}
                                                    <div class="relative group/roles flex items-center justify-end">
                                                        <div class="flex items-center gap-1.5 cursor-help">
                                                            {project.data.role
                                                                .length > 0 && (
                                                                <span class="text-[10px] font-medium bg-muted/30 px-1.5 py-0.5 text-muted-foreground uppercase tracking-wider">
                                                                    {
                                                                        project
                                                                            .data
                                                                            .role[0]
                                                                    }
                                                                </span>
                                                            )}
                                                            {project.data.role
                                                                .length > 1 && (
                                                                <span class="text-[10px] text-muted-foreground font-medium">
                                                                    +
                                                                    {project
                                                                        .data
                                                                        .role
                                                                        .length -
                                                                        1}
                                                                </span>
                                                            )}
                                                        </div>

                                                        {/* Hover Popover */}
                                                        <div class="absolute bottom-full right-0 mb-2 w-max hidden group-hover/roles:flex flex-col gap-1 p-2 bg-popover shadow-lg z-20">
                                                            {project.data.role.map(
                                                                (role) => (
                                                                    <span class="text-xs text-popover-foreground">
                                                                        {role}
                                                                    </span>
                                                                ),
                                                            )}
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </a>
                                    ))}
                                </div>
                            </section>
                        ),
                    )}
                </div>

                <footer class="py-6 flex flex-col items-center gap-4 text-sm text-muted-foreground mt-12">
                    <div class="flex flex-col items-center gap-1 text-center">
                        <p class="font-medium tracking-wide">
                            &copy; {new Date().getFullYear()} {PROFILE.name}.
                            All rights reserved.
                        </p>
                        <a
                            href={PROFILE.links.email}
                            class="hover:text-primary transition-colors"
                        >
                            {PROFILE.email}
                        </a>
                    </div>

                    <button
                        id="back-to-top"
                        class="p-2 hover:bg-muted transition-all opacity-0 translate-y-4 data-[visible=true]:opacity-100 data-[visible=true]:translate-y-0"
                        aria-label="Back to top"
                        title="Back to top"
                    >
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="lucide lucide-arrow-up"
                        >
                            <path d="m18 15-6-6-6 6" />
                        </svg>
                    </button>
                </footer>
            </main>
        )
    }

    {
        /* =========================================
        LAYOUT 2: Left Aligned / Bax Style
        ========================================= */
    }
    {
        CURRENT_LAYOUT === 2 && (
            <main class="container mx-auto px-6 py-12 max-w-[1800px] animate-blur-in">
                <div class="absolute right-6 top-6 z-50">
                    <ThemeToggle />
                </div>

                {/* Header Section */}
                <header class="mb-20 pt-10 md:pt-20">
                    <div class="flex flex-col md:flex-row gap-8 items-start">
                        <div class="profile-flip-container w-32 h-32 md:w-40 md:h-40 shrink-0 rounded-xl border border-border/50 cursor-pointer [perspective:1000px] z-50 group hover:scale-110 transition-transform duration-500">
                            <div class="profile-flip-inner w-full h-full relative transition-[transform] duration-700 ease-in-out [transform-style:preserve-3d]">
                                <ResponsiveImage
                                    src={PROFILE.avatar}
                                    alt="Profile"
                                    class="object-cover w-full h-full absolute inset-0 rounded-xl bg-muted [backface-visibility:hidden]"
                                    decoding="async"
                                    fetchpriority="high"
                                    loading="eager"
                                    sizes="10rem"
                                />
                                <ResponsiveImage
                                    src="/melogo.png"
                                    alt="Logo"
                                    class="object-cover w-full h-full absolute inset-0 rounded-xl bg-white [backface-visibility:hidden] flip-180"
                                    loading="lazy"
                                    decoding="async"
                                    sizes="10rem"
                                />
                            </div>
                        </div>

                        <div class="space-y-5 max-w-3xl pt-2">
                            <h1 class="text-5xl md:text-7xl font-black tracking-tighter text-foreground font-display uppercase animate-blur-in delay-200">
                                <a href="/about" class="hover:text-primary transition-colors">
                                    {PROFILE.name}
                                </a>
                            </h1>

                            <div class="space-y-1 text-base md:text-lg text-muted-foreground animate-blur-in delay-300">
                                <p class="font-medium text-foreground">
                                    {PROFILE.role}
                                </p>
                                <p>Contact: {PROFILE.email}</p>
                                <p class="text-sm md:text-base leading-relaxed pt-2 max-w-2xl text-muted-foreground/80">
                                    {PROFILE.bio}
                                </p>
                            </div>
                        </div>
                    </div>
                </header>

                <div class="w-full h-px bg-border/40 mb-12" />

                {/* Works Section */}
                <div class="space-y-4 relative">
                    {orderedGroupedPortfolio.map(
                        ([genre, projects]) => (
                            <section class="mb-24">
                                <h3 class="text-xl font-bold tracking-tight mb-8 text-foreground/90">
                                    {genre} Designs
                                </h3>

                                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-12">
                                    {projects.map((project, index) => (
                                        <a
                                            href={`/work/${project.slug}`}
                                            class={`group block space-y-4 cursor-pointer animate-blur-in`}
                                            style={`animation-delay: ${Math.min((index + 1) * 100 + 400, 1000)}ms`}
                                        >
                                            {/* Thumbnail */}
                                            <div class="relative aspect-video overflow-hidden bg-muted rounded-md border border-border/50 transition-all duration-500 group-hover:border-primary/50">
                                                <ResponsiveImage
                                                    src={project.data.thumbnail}
                                                    alt={project.data.title}
                                                    class="object-cover w-full h-full transition-transform duration-700 group-hover:scale-105"
                                                    loading="lazy"
                                                    decoding="async"
                                                    sizes="(max-width: 768px) 92vw, (max-width: 1200px) 44vw, 30vw"
                                                />
                                                {/* Overlay */}
                                                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-4">
                                                    {/* Optional overlay content */}
                                                </div>
                                            </div>

                                            {/* Info */}
                                            <div class="flex justify-between items-start gap-4">
                                                <div class="space-y-1">
                                                    <h4 class="font-bold text-lg leading-tight group-hover:text-primary transition-colors">
                                                        {project.data.title}
                                                    </h4>
                                                    <p class="text-xs text-muted-foreground/80">
                                                        {project.data.client}
                                                    </p>
                                                </div>
                                                <span class="text-xs font-mono text-muted-foreground/60">
                                                    {new Date(
                                                        project.data.date,
                                                    ).getFullYear()}
                                                </span>
                                            </div>
                                        </a>
                                    ))}
                                </div>
                                <div class="flex items-center gap-4 mb-10" />
                            </section>
                        ),
                    )}
                </div>

                <footer class="py-12 border-t border-border/40 mt-20">
                    <div class="flex justify-between items-center text-sm text-muted-foreground">
                        <p>
                            &copy; {new Date().getFullYear()} {PROFILE.name}.
                            All rights reserved.
                        </p>
                        <div class="flex gap-4">
                            <a
                                href={PROFILE.links.twitter}
                                class="hover:text-foreground"
                            >
                                Twitter
                            </a>
                            <a
                                href={PROFILE.links.email}
                                class="hover:text-foreground"
                            >
                                Email
                            </a>
                        </div>
                    </div>
                </footer>
            </main>
        )
    }

    {
        /* =========================================
        LAYOUT 3: Split View (Modern Sidebar)
        ========================================= */
    }
    {
        CURRENT_LAYOUT === 3 && (
            <div class="portfolio-home relative min-h-screen overflow-hidden bg-background">
                <PortfolioScene
                    client:media="(min-width: 1100px) and (prefers-reduced-motion: no-preference)"
                    projectCount={sortedPortfolio.length}
                    categories={genreNames}
                    featuredProjects={featuredSceneProjects}
                />

                <div class="portfolio-shell relative z-10 grid min-h-screen grid-cols-1 lg:grid-cols-[minmax(300px,36vw)_1fr]">
                    <aside class="portfolio-hero-panel flex min-h-[540px] flex-col justify-between border-b border-border/40 bg-background/82 px-6 py-6 backdrop-blur-md sm:px-8 lg:sticky lg:top-0 lg:h-screen lg:border-b-0 lg:border-r lg:px-10 lg:py-8 xl:px-12">
                        <div>
                            <div class="flex items-start justify-between gap-4 animate-blur-in delay-100">
                                <a
                                    href="/about"
                                    class="profile-flip-container group relative z-50 h-20 w-20 shrink-0 cursor-pointer border border-border/70 [perspective:1000px] transition-transform duration-500 hover:scale-110"
                                    aria-label="About OFAUNS"
                                >
                                    <div class="profile-flip-inner relative h-full w-full transition-[transform] duration-700 ease-in-out [transform-style:preserve-3d]">
                                        <ResponsiveImage
                                            src={PROFILE.avatar}
                                            alt="OFAUNS profile"
                                            class="absolute inset-0 h-full w-full bg-muted object-cover [backface-visibility:hidden]"
                                            decoding="async"
                                            fetchpriority="high"
                                            loading="eager"
                                            sizes="5rem"
                                        />
                                        <ResponsiveImage
                                            src="/melogo.png"
                                            alt="OFAUNS mark"
                                            class="flip-180 absolute inset-0 h-full w-full bg-white object-cover [backface-visibility:hidden]"
                                            loading="lazy"
                                            decoding="async"
                                            sizes="5rem"
                                        />
                                    </div>
                                </a>
                                <ThemeToggle />
                            </div>

                            <div class="mt-10 max-w-xl space-y-4 sm:mt-12 lg:mt-12">
                                <h1 class="animate-blur-in delay-200 max-w-none text-5xl font-black uppercase leading-[0.9] tracking-normal text-foreground sm:text-6xl xl:text-7xl">
                                    <a
                                        href="/about"
                                        class="inline-block transition-colors duration-500 hover:text-primary"
                                    >
                                        {PROFILE.name}
                                    </a>
                                </h1>
                                <p class="animate-blur-in delay-300 max-w-sm border-l border-border/60 pl-4 text-sm font-semibold uppercase leading-6 tracking-[0.16em] text-muted-foreground sm:text-base">
                                    {PROFILE.role}
                                </p>
                            </div>
                        </div>

                        <div class="mt-8 space-y-4">
                            <nav class="portfolio-field-board space-y-3" aria-label="Portfolio categories">
                                <p class="text-[0.65rem] font-bold uppercase tracking-[0.2em] text-muted-foreground">
                                    Fields
                                </p>
                                <div class="portfolio-field-list">
                                    {genreNames.map((genre) => (
                                        <a
                                            href={`#${genre}`}
                                            data-scene-project="true"
                                            data-scene-title={genre}
                                            data-scene-genre={genre}
                                            data-scene-color={
                                                genreMeta[genre]?.color ||
                                                SCENE_COLORS[0]
                                            }
                                            class="portfolio-chip portfolio-field-sign"
                                            style={`--scene-accent: ${genreMeta[genre]?.color || SCENE_COLORS[0]}`}
                                        >
                                            <span class="portfolio-field-index">
                                                {String(
                                                    genreMeta[genre]?.index || 1,
                                                ).padStart(2, "0")}
                                            </span>
                                            <span class="portfolio-field-name">
                                                {genre}
                                            </span>
                                            <span class="portfolio-field-count">
                                                {groupedPortfolio[genre]?.length || 0}
                                            </span>
                                        </a>
                                    ))}
                                </div>
                            </nav>

                            <div class="flex flex-wrap items-center gap-5 border-t border-border/40 pt-4 text-sm text-muted-foreground">
                                <a
                                    href={PROFILE.links.email}
                                    class="font-bold uppercase tracking-[0.18em] text-foreground hover:text-primary"
                                >
                                    Email
                                </a>
                                <a
                                    href={PROFILE.links.twitter}
                                    class="font-bold uppercase tracking-[0.18em] text-foreground hover:text-primary"
                                >
                                    X
                                </a>
                            </div>
                        </div>
                    </aside>

                    <main id="work" class="portfolio-workstream px-5 py-10 sm:px-8 lg:px-12 lg:py-14 xl:px-16">
                        <section class="mb-14 max-w-3xl space-y-5">
                            {/* <p class="text-xs font-bold uppercase tracking-[0.28em] text-primary">
                                Selected Work
                            </p>
                            <h2 class="text-3xl font-bold leading-tight tracking-normal text-foreground sm:text-4xl lg:text-5xl">
                                Motion-led portfolio for visual systems, films,
                                web interfaces, and interactive design.
                            </h2> */}
                        </section>

                        <div class="space-y-20">
                            {orderedGroupedPortfolio.map(
                                ([genre, projects], genreIndex) => (
                                    <section
                                        id={genre}
                                        class="scroll-mt-20"
                                        style={`--scene-accent: ${genreMeta[genre]?.color || SCENE_COLORS[0]}`}
                                    >
                                        <div class="mb-5 flex items-end justify-between gap-4 border-b border-border/45 pb-4">
                                            <div class="space-y-2">
                                                <p class="font-mono text-xs text-muted-foreground">
                                                    {String(
                                                        genreIndex + 1,
                                                    ).padStart(2, "0")}
                                                </p>
                                                <h2 class="text-2xl font-black uppercase leading-none tracking-normal text-foreground sm:text-4xl">
                                                    {genre}
                                                </h2>
                                            </div>
                                            <span class="font-mono text-xs uppercase tracking-[0.16em] text-muted-foreground">
                                                {projects.length} Projects
                                            </span>
                                        </div>

                                        <div class="grid grid-cols-1 gap-4">
                                            {projects.map((project, index) => {
                                                const projectPresentation =
                                                    getProjectPresentation(
                                                        project,
                                                    );

                                                return (
                                                <a
                                                    href={`/work/${project.slug}`}
                                                    data-scene-project="true"
                                                    data-scene-title={
                                                        project.data.title
                                                    }
                                                    data-scene-genre={genre}
                                                    data-scene-color={
                                                        genreMeta[genre]
                                                            ?.color ||
                                                        SCENE_COLORS[0]
                                                    }
                                                    class={`portfolio-project portfolio-project--${projectPresentation.layout} group grid gap-5 border-t border-border/35 bg-background/28 p-3 backdrop-blur-sm transition-all duration-500 hover:border-[var(--scene-accent)] hover:bg-background/55 sm:p-4 md:grid-cols-[minmax(0,1.15fr)_minmax(220px,0.85fr)]`}
                                                    style={`--scene-accent: ${genreMeta[genre]?.color || SCENE_COLORS[0]}; --reveal-delay: ${Math.min((index % 4) * 90, 270)}ms`}
                                                >
                                                    <div
                                                        class="portfolio-project-thumb relative overflow-hidden bg-muted"
                                                        data-fit={
                                                            projectPresentation.imageFit
                                                        }
                                                    >
                                                        <ResponsiveImage
                                                            src={
                                                                project.data
                                                                    .thumbnail
                                                            }
                                                            alt={
                                                                project.data
                                                                    .title
                                                            }
                                                            class="h-full w-full object-cover transition-transform duration-700 ease-out group-hover:scale-105"
                                                            loading="lazy"
                                                            decoding="async"
                                                            sizes="(max-width: 768px) 92vw, (max-width: 1024px) 78vw, 38vw"
                                                        />
                                                        <div class="absolute inset-0 bg-gradient-to-t from-black/65 via-black/10 to-transparent opacity-70 transition-opacity duration-500 group-hover:opacity-95" />
                                                        <span class="portfolio-project-mode">
                                                            {
                                                                projectPresentation.modeLabel
                                                            }
                                                        </span>
                                                        <div class="absolute bottom-4 left-4 flex items-center gap-3 text-white">
                                                            <span class="portfolio-project-view-line h-px w-10" />
                                                            <span class="text-xs font-bold uppercase tracking-[0.2em]">
                                                                View
                                                            </span>
                                                        </div>
                                                    </div>

                                                    <div class="flex min-h-[220px] flex-col justify-between gap-8 p-1 sm:p-2">
                                                        <div class="space-y-5">
                                                            <div class="flex items-center justify-between gap-4 font-mono text-xs text-muted-foreground">
                                                                <span>
                                                                    {String(
                                                                        index +
                                                                            1,
                                                                    ).padStart(
                                                                        2,
                                                                        "0",
                                                                    )}
                                                                </span>
                                                                <span>
                                                                    {new Date(
                                                                        project
                                                                            .data
                                                                            .date,
                                                                    ).getFullYear()}
                                                                </span>
                                                            </div>
                                                            <h3 class="portfolio-project-title text-2xl font-black uppercase leading-tight tracking-normal text-foreground transition-colors duration-300 group-hover:text-[var(--scene-accent)] sm:text-3xl">
                                                                {getBalancedTitleLines(
                                                                    project.data
                                                                        .title,
                                                                    {
                                                                        maxLines: 3,
                                                                        targetLength: 18,
                                                                    },
                                                                ).map(
                                                                    (line) => (
                                                                        <span>
                                                                            {line}
                                                                        </span>
                                                                    ),
                                                                )}
                                                            </h3>
                                                            <p class="line-clamp-3 text-sm leading-6 text-muted-foreground">
                                                                {
                                                                    projectPresentation.homeIntro
                                                                }
                                                            </p>
                                                        </div>

                                                        <div class="flex items-center justify-between border-t border-border/35 pt-4 text-xs uppercase tracking-[0.16em] text-muted-foreground">
                                                            <span>
                                                                {project.data.client}
                                                            </span>
                                                            <span class="portfolio-project-open">
                                                                Open
                                                            </span>
                                                        </div>
                                                    </div>
                                                </a>
                                                );
                                            })}
                                        </div>
                                    </section>
                                ),
                            )}
                        </div>
                    </main>
                </div>
            </div>
        )
    }
</Layout>
